<%- include("../partials/header.ejs") %>
<main>
        <div class="future">
            <h1 style="color: white; font-weight: bold; text-align: center;">NO FUTURE EVENTS AS OF NOW!</h1>   
        </div>
    <!-- <% if(events.length > 0) { %>
        
        <% for(let event of events) { %>
        <div>
            <%= event.name %>
        </div>
        <div>
            <img src="<%= event.img %> " alt="PCI TEST">
        </div>
        <div>
            <%= event.info %>
        </div>
        <% } %>
    
    <% }else{ %>
    <div>
        No events
    </div>
    <% } %> -->
</main>

<%- include("../partials/footer.ejs") %>