<%- include("../partials/header.ejs") %>
<main>
    <div style="margin-top: 100px; text-align: center;">
        <h1 style="font-size: 4rem; font-weight: bold; margin-bottom: 50px;">Core Members</h1>
    </div>
    <% if(cores.length > 0) { %>
        <div class="container cores">
            <div class="row">
        <% for(let core of cores) { %>
            <div class="col-md-4">
                <div class="card">
                    <img src="<%= core.img %> " class="card-img-top" alt="PCI TEST">
                    <div class="card-body card_body">
                        <h5 class="card-title">Name: <%= core.name %></h5>
                        <p class="card-text">Post: <%= core.post %></p>
                    </div>
                </div>
            </div>
        <% } %>
    </div>
    </div>
    <% }else{ %>
    <div>
        No events
    </div>
    <% } %>
</main>

<%- include("../partials/footer.ejs") %>